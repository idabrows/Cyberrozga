<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cyberrozga">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.teacher.GradesListActivity"></activity>
        <activity android:name=".view.activities.teacher.StudentsList" />
        <activity android:name=".view.activities.teacher.ChoosePanelActivity" />
        <activity android:name=".view.activities.common.GradesPreview" />
        <activity android:name=".view.activities.common.SubjectsActivity" />
        <activity
            android:name=".view.activities.common.Mails"
            android:label="@string/title_activity_mails"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.activities.officeWorker.GroupManagement" />
        <activity android:name=".view.activities.officeWorker.OfficePanelActivity" />
        <activity android:name=".view.activities.teacher.TeacherInbox" />
        <activity
            android:name=".view.activities.teacher.MailToParents"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity android:name=".view.activities.teacher.RegisterActivity" />
        <activity android:name=".view.activities.teacher.TeacherPanelActivity" />
        <activity android:name=".view.activities.parent.ParentInbox" />
        <activity android:name=".view.activities.parent.MailToTeacher" />
        <activity android:name=".view.activities.parent.ChildrenPanelActivity" />
        <activity android:name=".view.activities.pupil.StudentPanelActivity" />
        <activity android:name=".view.activities.parent.ParentPanelActivity" />
        <activity
            android:name=".view.activities.common.LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateVisible|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.teacher.EditMarkPopUp"
            android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>