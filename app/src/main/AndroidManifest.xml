<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cyberrozga">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activities.Mails"
            android:label="@string/title_activity_mails"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".view.activities.GroupManagement" />
        <activity android:name=".view.activities.OfficePanelActivity" />
        <activity android:name=".view.activities.Teacher.TeacherInbox" />
        <activity android:name=".view.activities.Teacher.MailToParents" />
        <activity android:name=".view.activities.RegisterActivity" />
        <activity android:name=".view.activities.Teacher.TeacherPanelActivity" />
        <activity android:name=".view.activities.Parent.ParentInbox" />
        <activity android:name=".view.activities.Parent.MailToTeacher" />
        <activity android:name=".view.activities.ChildrenPanelActivity" />
        <activity android:name=".view.activities.StudentPanelActivity" />
        <activity android:name=".view.activities.Parent.ParentPanelActivity" />
        <activity
            android:name=".view.activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="stateVisible|adjustPan"/>
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.EditMarkPopUp"
            android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>